 SELECT MinorUnitInDecimal, barcodeno,itemid, stockid,itemname,DATE_FORMAT(stockdate,'%d-%b-%Y') stockdate,batchnumber,IsExpirable,IF(expirydate='0001-01-01','', DATE_FORMAT(expirydate,'%d-%b-%Y')) ExpiryDate  ,(`InitialCount` - `ReleasedCount` - `PendingQty` ) InHandQty,minorunit,locationid,panel_id  FROM st_nmstock WHERE locationid=10 AND (`InitialCount` - `ReleasedCount` - `PendingQty` )>0 AND ispost=1  ORDER BY stockdate 